#!/bin/zsh
THISDIR=${0:a:h}

if [[ -d $HOME/.config/qutebrowser ]] && [[ ! -L $HOME/.config/qutebrowser/autoconfig.yml ]];then
  echo linking 
  ln -s $THISDIR/autoconfig.yml $HOME/.config/qutebrowser/
fi
